import React from 'react';

const ScrollBar = () => {
  return (
    <div className="scroll-bar h-64 w-4 bg-gray-200 fixed right-0 top-0">
      {/* Scroll events and sync with date selection */}
    </div>
  );
};

export default ScrollBar;
