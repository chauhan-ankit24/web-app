import React from 'react';

const BottomNavBar = () => {
    return (
        <footer className="bottom-0 left-0 right-0 bg-gray-800 text-white p-4 flex justify-around">
            <button>🏠</button>
            <button>📅</button>
            <button>⚙️</button>
            <button>🔔</button>
        </footer>
    );
};

export default BottomNavBar;
